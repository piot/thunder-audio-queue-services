# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(thunder-audio-queue-services STATIC sound_driver.c)

include(Tornado.cmake)
set_tornado(thunder-audio-queue-services)

# Core Audio / Audio Queue Service
if(OS_MACOS)
  message("MacOS: Add AudioToolbox, CoreFoundation")
  find_library(COREFOUNDATION_LIBRARY CoreFoundation)
  find_library(COREAUDIO_TOOLBOX_LIBRARY AudioToolbox)
  # find_library(COCOA_LIBRARY Cocoa) find_library(IOKIT_LIBRARY IOKIT)

  target_link_libraries(
    thunder-audio-queue-services PUBLIC ${COREFOUNDATION_LIBRARY}
                                        ${COREAUDIO_TOOLBOX_LIBRARY})
  # target_link_libraries(thunder-audio-queue-services PUBLIC ${COCOA_LIBRARY})
  # # needed for glfw target_link_libraries(thunder-audio-queue-services PUBLIC
  # ${IOKIT_LIBRARY}) # needed for glfw
endif(OS_MACOS)

target_include_directories(thunder-audio-queue-services PUBLIC ../include)

target_link_libraries(thunder-audio-queue-services PUBLIC thunder)
